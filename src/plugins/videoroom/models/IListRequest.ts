// To get a list of the available rooms (excluded those configured or created as private rooms)
// you can make use of the list request, which has to be formatted as follows:
export interface IListRequest {
	"request": "list"
}

